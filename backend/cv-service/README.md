# CV Service

The CV service handles computer-vision processing for OmniScan AR — object/marker detection and scan analysis that feeds results back to the backend for the AR experience. It is expected to be a Python service; once implemented, this section will document setup (virtualenv/`requirements.txt`) and run commands.
