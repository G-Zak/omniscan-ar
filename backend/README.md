# Backend

The backend is the central API and orchestration service for OmniScan AR — it handles authentication, persists scan/session data, and coordinates communication between the `cv-service`, `dashboard`, and `unity-client` components. Build-out begins in a later sprint; once implemented, this section will document the run/build commands (expected to be a JVM-based service, see `target/` in `.gitignore`).
