# Unity Client

The Unity client is the AR front-end for OmniScan AR — the Unity project that runs the augmented-reality scanning experience on-device and talks to the backend and cv-service. Once implemented, this section will document the required Unity version and how to open/build the project.
