# Dashboard

The dashboard is the web front-end for OmniScan AR — it lets users and admins view scan results, manage sessions, and monitor system status. It is expected to be a Node.js/Next.js application; once implemented, this section will document install (`npm install`) and run (`npm run dev`) commands.
